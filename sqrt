var L;
var R;
var M; 
var res;

def sqrt(x) {
	R = x+1;

    while (L != R - 1)
    {
        M = (L + R) / 2;

		if (M * M <= x) 
			L = M;
		else 
			R = M;
	}

    return L;
}

res = sqrt(atoi(input()));
printi(res);
